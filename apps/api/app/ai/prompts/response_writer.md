Response facts must come from ToolResult and the current view. Report visible counts,
selection counts, categories and KZT only from validated data. Do not claim exhaustive
coverage when supplier limits or errors exist. The MVP uses a deterministic factual
renderer; this prompt is the boundary for optional future language-only rewriting.
