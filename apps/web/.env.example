# Public FastAPI origin, without /api or a trailing slash.
# Local development may omit this value and falls back to http://127.0.0.1:8000.
# Vercel Production and Preview must use the public HTTPS backend origin.
API_BASE_URL=http://127.0.0.1:8000
